{
  "name": "be",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:lambda": "npx esbuild src/entry/lambda-entry.ts --outfile=dist/lambda-entry.js --platform=browser --target=es2023 --format=esm --bundle --minify --external:@aws-sdk --external:@smithy",
    "test": "npx esbuild test/*.test.ts --outdir=dist --platform=browser --target=es2023 --format=esm --bundle && cd .. && ./llrt test -d be/dist"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.839.0",
    "db": "file:../db",
    "shared": "file:../shared"
  },
  "devDependencies": {
    "@aws-sdk/lib-dynamodb": "^3.839.0",
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^20.19.2",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3"
  }
}
