{
  "name": "be",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/dev-server.ts",
    "build": "bun build src/lambda/index.ts --outdir dist --target node --minify",
    "build:llrt": "esbuild src/lambda/index.ts --platform=browser --target=es2023 --format=esm --bundle --minify --external:@aws-sdk --external:@smithy --external:bun:sqlite --outfile=dist/index.js",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "db": "file:../db",
    "shared": "file:../shared"
  },
  "devDependencies": {
    "@aws-sdk/types": "^3.664.0",
    "@types/bun": "^1.2.17",
    "esbuild": "^0.24.0",
    "typescript": "^5.8.3"
  }
}
