{
  "name": "be",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:lambda": "npx esbuild src/lambda-entry.ts --outfile=dist/lambda-entry.js --platform=browser --target=es2022 --format=esm --bundle --minify --external:@aws-sdk --external:@smithy"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}