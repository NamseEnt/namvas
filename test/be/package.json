{
  "name": "be",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "esbuild src/local-entry.ts --outfile=dist/local-entry.js --platform=browser --target=es2022 --format=esm --bundle --minify --external:@aws-sdk --external:@smithy --watch",
    "build:lambda": "esbuild src/lambda-entry.ts --outfile=dist/lambda-entry.js --platform=browser --target=es2022 --format=esm --bundle --minify --external:@aws-sdk --external:@smithy"
  },
  "dependencies": {
    "esbuild": "^0.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}